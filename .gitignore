# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# raw source export for data/newly-added-products.json — only the converted
# JSON is committed, not the original CSV
/Newly Added Products*.csv
